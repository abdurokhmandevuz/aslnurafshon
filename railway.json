{
  "build": {
    "command": "cd backend && pip install -r requirements.txt && python manage.py collectstatic --noinput"
  },
  "start": {
    "command": "cd backend && python manage.py migrate && (BOT_WEBHOOK_URL= python -m bot.main &) && gunicorn config.wsgi:application --bind 0.0.0.0:${PORT}"
  },
  "staticFilesDirectory": "frontend"
}
